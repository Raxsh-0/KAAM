<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this only styles the window before setContent runs. -->
    <style name="Theme.Kindred" parent="android:Theme.Material.NoActionBar" />
</resources>
